<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GPL Mods - Modded Games and Software</title>
    
    <!-- Link to our Global CSS -->
    <link rel="stylesheet" href="/css/style.css">

    <!-- Google Fonts: Roboto -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
    
    <!-- Font Awesome for Icons -->
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>

    <!-- Google reCAPTCHA Script -->
    <script src="https://www.google.com/recaptcha/api.js" async defer></script>

    <!-- Cashfree SDK Script -->
    <script src="https://sdk.cashfree.com/js/v3/cashfree.js"></script>
</head>
<body>

    <header class="main-header">
        <nav class="container" style="display: flex; justify-content: space-between; align-items: center; position: relative;">
            <div style="display: flex; align-items: center; gap: 1.5rem;">
                <a href="/" class="logo">
                    <h1>GPL Mods</h1>
                </a>
                
        
            <!-- A container for all the navigation elements -->
            <div class="navigation-wrapper">
        
                <!-- ===== 1. DESKTOP NAVIGATION ===== -->
                <div class="desktop-nav" style="display: flex; align-items: center; gap: 1rem;">
                    <!-- Login/User Buttons -->
                    <div class="user-actions">
                        <% if (user) { %>
                            <% if (user.profileImageUrl) { %>
                                <img src="<%= user.profileImageUrl %>" alt="<%= user.username %>'s avatar" style="width: 40px; height: 40px; border-radius: 50%; object-fit: cover; margin-right: 1rem; vertical-align: middle;">
                            <% } %>
                            <span style="margin-right: 1rem;">Welcome, <%= user.username %></span>
                            <a href="/profile" class="btn btn-secondary">My Profile</a>
                            <a href="/upload" class="btn btn-primary">Upload Mod</a>
                            <a href="/logout" class="btn btn-secondary">Logout</a>
                        <% } else { %>
                            <a href="/login" class="btn btn-secondary">Login</a>
                            <a href="/register" class="btn btn-primary">Sign Up</a>
                        <% } %>
                    </div>
                </div>
                
                <!-- ===== 2. HAMBURGER BUTTON (MOBILE ONLY) ===== -->
                <button id="hamburger-btn" aria-label="Open menu">
                    <span class="hamburger-bar"></span>
                    <span class="hamburger-bar"></span>
                    <span class="hamburger-bar"></span>
                </button>
            </div>
        
            <!-- ===== 3. MOBILE NAVIGATION MENU (HIDDEN BY DEFAULT) ===== -->
            <div id="mobile-nav-menu">
                <!-- Animated Search Bar is now here for mobile view -->
                <div id="animatedSearchBar" class="search-bar-container">
                    <form action="/search" method="GET" class="search-form">
                        <span class="search-icon">&#128269;</span>
                        <input type="text" id="searchInput" name="q" placeholder="Search for mods..." autocomplete="off">
                    </form>
                    <div id="searchSuggestions" class="search-suggestions"></div>
                    <div id="searchHistory" class="search-history"></div>
                </div>

                <!-- Music Controls for Mobile View -->
                <div id="music-controls-mobile" style="display: flex; align-items: center; justify-content: center; gap: 1rem; padding: 10px 0;">
                    <span>Music:</span>
                    <button id="play-music-btn-mobile" title="Play Music">▶️</button>
                    <button id="pause-music-btn-mobile" title="Pause Music">⏸️</button>
                </div>

                <a href="/profile">My Profile</a>
                <a href="/upload">Upload Mod</a>
                <a href="/community-chat">Community Chat</a>
                <hr>
                <% if (user) { %>
                    <a href="/logout">Logout</a>
                <% } else { %>
                    <a href="/login">Login</a>
                    <a href="/register">Sign Up</a>
                <% } %>
            </div>
        </nav>
    </header>

    <main>