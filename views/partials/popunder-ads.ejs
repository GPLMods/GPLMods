<!-- views/partials/popunder-ads.ejs -->
<% if (showAds) { %>
<script>
(function(wiwacw){
var d = document,
    s = d.createElement('script'),
    l = d.scripts[d.scripts.length - 1];
s.settings = wiwacw || {};
s.src = "\/\/fluffy-management.com\/c.D\/9F6ubS2y5ilGS_WzQn9ANojnkY2FObD-kUycM\/Ss0m2GO-TjYh4kOTT\/Ik1G";
s.async = true;
s.referrerPolicy = 'no-referrer-when-downgrade';
l.parentNode.insertBefore(s, l);
})({})
</script>
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            // 2. Define the elements that should trigger the pop-under when clicked
            const triggerElements = document.querySelectorAll('.mod-card-link, .download-button, .part-btn');
            
            triggerElements.forEach(el => {
                el.addEventListener('click', (e) => {
                    // 3. Call the HilltopAds function here.
                    // Look at their documentation. They usually provide a function you can call manually.
                    // Example (Replace with actual Hilltop function):
                    // if (typeof window.hilltopShowPopunder === 'function') {
                    //     window.hilltopShowPopunder();
                    // }
                    
                    console.log("Ad Trigger: User clicked a targeted element.");
                    // The link will continue to work normally, but the pop-under script will have been triggered.
                });
            });
        });
    </script>
<% } %>