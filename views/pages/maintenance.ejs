<!-- This page is special: it doesn't include the regular header/footer -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GPL Mods - Under Maintenance</title>
    <!-- We can inline some simple styles -->
    <style>
        :root { --black: #0a0a0a; --dark-grey: #1a1a1a; --white: #f5f5f5; --gold: #FFD700; }
        body { background-color: var(--black); color: var(--white); font-family: 'Roboto', sans-serif; display: flex; align-items: center; justify-content: center; height: 100vh; margin: 0; }
        .container { text-align: center; padding: 2rem; border: 1px solid var(--dark-grey); border-radius: 8px; }
        h1 { font-size: 3rem; color: var(--gold); }
    </style>
</head>
<body>
    <div class="container">
        <h1>Under Maintenance</h1>
        <p style="font-size: 1.2rem;">
            GPL Mods is currently down for scheduled maintenance.
        </p>
        <p>We'll be back online shortly. Thank you for your patience!</p>
    </div>
</body>
</html>