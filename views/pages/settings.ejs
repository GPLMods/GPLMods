<%-include('../partials/header') %>

<style>
    .settings-container { max-width: 800px; margin: 2rem auto 4rem auto; }
    .settings-section { background: var(--dark-gray); border: 1px solid #333; border-radius: 12px; padding: 30px; margin-bottom: 25px; display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 20px; transition: box-shadow 0.3s ease; }
    .settings-section:hover { box-shadow: 0 5px 20px rgba(0,0,0,0.4); }
    .settings-info h3 { margin: 0 0 8px 0; color: var(--white); font-size: 1.4em; display: flex; align-items: center; gap: 10px; }
    .settings-info p { margin: 0; color: var(--silver); font-size: 1em; line-height: 1.5; max-width: 500px; }
    
    .btn-danger-outline { background: transparent; border: 2px solid var(--red); color: var(--red); padding: 12px 25px; border-radius: 8px; font-weight: bold; cursor: pointer; transition: 0.3s; font-family: 'Poppins', sans-serif; font-size: 1em; }
    .btn-danger-outline:hover { background: var(--red); color: var(--white); box-shadow: 0 0 15px var(--glow-red); }
</style>

<main class="container">
    <div class="settings-container">
        
        <!-- ======== DYNAMIC GO BACK BUTTON ======== -->
        <div class="back-btn-wrapper" style="margin-bottom: 20px;">
            <a href="javascript:history.back()" class="dynamic-back-btn">
                <i class="fas fa-arrow-left"></i> Go Back
            </a>
        </div>
        <!-- ======================================== -->

        <div style="text-align: center; padding: 10px 0 40px;">
            <h1 style="color: var(--white); font-size: 2.8em; margin-bottom: 10px;">Account <span style="color: var(--gold); text-shadow: 0 0 15px var(--glow-gold);">Settings</span></h1>
        </div>

        <!-- 2FA Settings (RESTORED) -->
        <div class="settings-section">
            <div class="settings-info">
                <h3><i class="fas fa-shield-alt" style="color: var(--gold);"></i> Two-Factor Auth (2FA)</h3>
                <p>Add an extra layer of security to your account.</p>
            </div>
            <form action="<%= user.twoFactorEnabled ? '/account/2fa/disable' : '/account/2fa/setup' %>" method="<%= user.twoFactorEnabled ? 'POST' : 'GET' %>" style="margin: 0;">
                <label class="switch">
                    <input type="checkbox" onChange="this.form.submit()" <%= user.twoFactorEnabled ? 'checked' : '' %>>
                    <span class="slider round"></span>
                </label>
            </form>
        </div>

        <div class="settings-section">
            <div class="settings-info">
                <h3><i class="fas fa-bell" style="color: var(--gold);"></i> New Login Alerts</h3>
                <p>Receive an email when a new device signs in to your account.</p>
            </div>
            <form action="/settings/safety-emails" method="POST" style="margin: 0;">
                <input type="hidden" name="settingType" value="loginAlertEmailsEnabled">
                <label class="switch">
                    <input type="checkbox" name="loginAlertEmailsEnabled" onchange="this.form.submit()" <%= user.loginAlertEmailsEnabled ? 'checked' : '' %>>
                    <span class="slider round"></span>
                </label>
            </form>
        </div>

        <div class="settings-section">
            <div class="settings-info">
                <h3><i class="fas fa-cloud-upload-alt" style="color: var(--blue);"></i> Draft Auto-save</h3>
                <p>Automatically save changes while editing a mod. Drafts remain private until you submit them.</p>
            </div>
            <form action="/settings/auto-save" method="POST" style="margin: 0;">
                <label class="switch">
                    <input type="checkbox" name="autoSaveEnabled" onchange="this.form.submit()" <%= user.autoSaveEnabled ? 'checked' : '' %>>
                    <span class="slider round"></span>
                </label>
            </form>
        </div>

        <div class="settings-section">
            <div class="settings-info">
                <h3><i class="fas fa-eye-slash" style="color: var(--gold);"></i> Filter 18+ Mods</h3>
                <p>Hide mods marked 18+ across the site. This is enabled by default for members under 18 and disabled by default for adults.</p>
            </div>
            <form action="/settings/adult-content" method="POST" style="margin: 0;">
                <label class="switch">
                    <input type="checkbox" name="hideAdultContent" onchange="this.form.submit()" <%= user.hideAdultContent ? 'checked' : '' %>>
                    <span class="slider round"></span>
                </label>
            </form>
        </div>

        <div class="settings-section">
            <div class="settings-info">
                <h3><i class="fas fa-shield-alt" style="color: var(--red);"></i> Failed Login Alerts</h3>
                <p>Receive an email after five incorrect password attempts on your account.</p>
            </div>
            <form action="/settings/safety-emails" method="POST" style="margin: 0;">
                <input type="hidden" name="settingType" value="failedLoginAlertEmailsEnabled">
                <label class="switch">
                    <input type="checkbox" name="failedLoginAlertEmailsEnabled" onchange="this.form.submit()" <%= user.failedLoginAlertEmailsEnabled ? 'checked' : '' %>>
                    <span class="slider round"></span>
                </label>
            </form>
        </div>

        <!-- Newsletter Settings -->
        <div class="settings-section">
            <div class="settings-info">
                <h3><i class="fas fa-newspaper" style="color: var(--blue);"></i> Newsletter Subscription</h3>
                <p>Receive updates about exclusive new mods and platform news directly to your inbox.</p>
            </div>
            <div>
                <% if (user.isSubscribedToNewsletter) { %>
                    <!-- Assuming you have this route built -->
                    <form action="/account/newsletter" method="POST" style="margin: 0;">
                        <input type="hidden" name="subscribe" value="false">
                        <button type="submit" class="btn btn-secondary" style="border-color: var(--silver); color: var(--silver);">Unsubscribe</button>
                    </form>
                <% } else { %>
                    <button type="button" id="open-newsletter-modal" class="btn btn-primary" style="background: var(--blue); color: var(--white); border: none; box-shadow: 0 0 15px rgba(33, 150, 243, 0.4);">Subscribe Now</button>
                <% } %>
            </div>
        </div>

        <div class="settings-section">
            <div class="settings-info">
                <h3><i class="fas fa-music" style="color: var(--gold);"></i> Music Preferences</h3>
                <p>Customize how background and theme music behave across the site.</p>
            </div>
            <div style="display: flex; gap: 20px; flex-wrap: wrap;">
                <div style="display: flex; align-items: center; gap: 12px;">
                    <label class="switch">
                        <input type="checkbox" id="setting-allow-theme" <%= user.musicSettings && user.musicSettings.allowThemeMusic ? 'checked' : '' %>>
                        <span class="slider round"></span>
                    </label>
                    <span style="color: var(--white); font-size: 0.9em; font-weight: 500;">Allow Theme Music</span>
                </div>
                <div style="display: flex; align-items: center; gap: 12px;">
                    <label class="switch">
                        <input type="checkbox" id="setting-autoplay-theme" <%= user.musicSettings && user.musicSettings.autoPlayTheme ? 'checked' : '' %>>
                        <span class="slider round"></span>
                    </label>
                    <span style="color: var(--white); font-size: 0.9em; font-weight: 500;">Auto-play Theme Music</span>
                </div>
            </div>
        </div>

        <!-- Web Storage & Cache Cleaner -->
        <div class="settings-section">
            <div class="settings-info">
                <h3><i class="fas fa-broom" style="color: var(--gold);"></i> Web Storage & Cache Cleaner</h3>
                <p>Purge outdated browser cache, cached scripts, stale service workers, and temporary client storage. Fast URL shortcut: navigate to <b>/cc</b> anytime.</p>
                <div id="cache-clean-status" style="margin-top: 8px; font-size: 0.85em; display: none;"></div>
            </div>
            <div>
                <button type="button" id="btn-purge-cache" class="btn btn-secondary" style="border-color: var(--gold); color: var(--gold); font-weight: 600; padding: 10px 20px; border-radius: 8px; cursor: pointer;">
                    <i class="fas fa-trash-alt" style="margin-right: 6px;"></i> Clear Web Cache Now
                </button>
            </div>
        </div>

        <!-- Account Actions (Danger Zone) -->

        <div class="settings-section" style="border-color: rgba(229, 57, 53, 0.5); background-color: rgba(229, 57, 53, 0.05); margin-top: 40px;">
            <div class="settings-info">
                <h3 style="color: var(--red);"><i class="fas fa-exclamation-triangle"></i> Danger Zone</h3>
                <p>Log out of your current session, permanently request account deletion, or leave the partnership program.</p>
            </div>
            <div style="display: flex; gap: 15px; flex-wrap: wrap;">
                <% if (user.role === 'distributor') { %>
                    <button type="button" id="open-leave-partnership-modal-btn" class="btn-danger-outline" style="border-color: var(--gold); color: var(--gold);">
                        <i class="fas fa-user-minus" style="margin-right: 8px;"></i> Leave Program
                    </button>
                <% } %>
                <button type="button" id="logout-btn" class="btn-danger-outline"><i class="fas fa-sign-out-alt" style="margin-right: 8px;"></i> Log Out</button>
                <button type="button" class="btn btn-primary" style="background: var(--red); color: var(--white); border: none; box-shadow: 0 0 15px var(--glow-red); font-size: 1em; padding: 12px 25px;" id="open-delete-modal-btn">Delete Account</button>
            </div>
        </div>
    </div>
</main>

<!-- Newsletter Modal -->
<div id="newsletter-modal" class="policy-modal">
    <div class="policy-modal-content" style="background-color: var(--dark-gray); padding: 40px 30px; border-radius: 15px; border-top: 4px solid var(--blue); text-align: center; box-shadow: 0 15px 40px rgba(0,0,0,0.6);">
        <i class="fas fa-envelope-open-text" style="font-size: 3.5em; color: var(--blue); margin-bottom: 20px;"></i>
        <h2 style="color: var(--white); margin-bottom: 15px; font-size: 1.8em;">Subscribe to Newsletter</h2>
        <p style="color: var(--silver); margin-bottom: 25px; line-height: 1.6;">Enter your email to receive weekly updates on top mods. We never spam, and you can unsubscribe at any time.</p>
        <form action="/account/newsletter" method="POST">
            <input type="hidden" name="subscribe" value="true">
            <input type="email" name="email" value="<%= user.email %>" required style="width: 100%; padding: 15px; border-radius: 8px; border: 1px solid #444; background: var(--black); color: white; margin-bottom: 25px; outline: none; font-family: 'Poppins', sans-serif; font-size: 1.05em;" placeholder="your@email.com">
            <div style="display: flex; gap: 15px; justify-content: center;">
                <button type="submit" class="btn btn-primary" style="background: var(--blue); color: white; border: none; padding: 12px 25px; font-size: 1em;">Confirm</button>
                <button type="button" id="cancel-newsletter-btn" class="btn btn-secondary" style="padding: 12px 25px; font-size: 1em;">Cancel</button>
            </div>
        </form>
    </div>
</div>

<!-- ======== LEAVE PARTNERSHIP MODAL ======== -->
<div id="leave-partnership-modal-container" class="policy-modal">
    <div class="policy-modal-content" style="background-color: var(--dark-gray); color: var(--white); padding: 40px 30px; text-align: center; border-radius: 15px; max-width: 500px; width: 90%; border-top: 4px solid var(--gold); box-shadow: 0 15px 40px rgba(0,0,0,0.6);">
        <i class="fas fa-user-slash" style="font-size: 3.5em; color: var(--gold); margin-bottom: 20px;"></i>
        <h2 style="color: var(--white); margin-bottom: 15px; font-size: 1.8em;">Leave Partnership Program?</h2>
        <p style="margin-bottom: 25px; color: var(--silver); line-height: 1.6;">
            Are you sure you want to leave the Distributor Partnership? <br><br>
            <strong>Warning:</strong> All your mods using external download links will be transferred to the <strong>GPL Community</strong>. You will still retain access to cloud upload mods.
        </p>
        <div style="display: flex; gap: 15px; justify-content: center;">
            <button type="button" id="confirm-leave-partnership-btn" class="btn btn-primary" style="background-color: var(--gold); color: var(--black); padding: 12px 30px; border-radius: 25px; font-weight: bold; cursor: pointer; border: none; box-shadow: 0 0 15px var(--glow-gold);">Yes, I want to leave</button>
            <button type="button" class="btn btn-secondary" id="cancel-leave-partnership-btn" style="background-color: transparent; color: var(--silver); border: 2px solid var(--silver); padding: 12px 30px; border-radius: 25px; font-weight: bold; cursor: pointer;">Cancel</button>
        </div>
    </div>
</div>

<!-- ======== CUSTOM LOGOUT MODAL ======== -->

<div id="logout-modal-container" class="policy-modal">
    <div class="policy-modal-content" style="background-color: var(--dark-gray); color: var(--white); padding: 40px 30px; text-align: center; border-radius: 15px; max-width: 400px; width: 90%; border-top: 4px solid var(--gold); box-shadow: 0 15px 40px rgba(0,0,0,0.6);">
        <i class="fas fa-sign-out-alt" style="font-size: 3.5em; color: var(--gold); margin-bottom: 20px;"></i>
        <h2 style="color: var(--white); margin-bottom: 15px; font-size: 1.8em;">Ready to leave?</h2>
        <p style="margin-bottom: 30px; color: var(--silver); line-height: 1.6;">
            Are you sure you want to log out of your account? You will need to enter your credentials next time you visit.
        </p>
        <div style="display: flex; gap: 15px; justify-content: center;">
            <a href="/logout" class="btn btn-primary" style="background-color: var(--gold); color: var(--black); padding: 12px 30px; border-radius: 25px; font-weight: bold; text-decoration: none; box-shadow: 0 0 15px var(--glow-gold);">Yes, Log Out</a>
            <button type="button" class="btn btn-secondary" id="cancelLogoutBtn" style="background-color: transparent; color: var(--silver); border: 2px solid var(--silver); padding: 12px 30px; border-radius: 25px; font-weight: bold; cursor: pointer;">Cancel</button>
        </div>
    </div>
</div>

<!-- ======== CUSTOM SECURE DELETE MODAL ======== -->
<div id="delete-modal-container" class="policy-modal">
    <div class="policy-modal-content" style="background-color: var(--dark-gray); color: var(--white); padding: 40px 30px; text-align: center; border-radius: 15px; max-width: 480px; width: 90%; border-top: 4px solid var(--red); box-shadow: 0 15px 40px rgba(0,0,0,0.6); position: relative;">
        
        <!-- Status Alert Banner -->
        <div id="delete-alert" style="display: none; padding: 12px 15px; border-radius: 8px; margin-bottom: 20px; font-size: 0.95em; text-align: center; line-height: 1.4;"></div>

        <!-- STEP 1: Warning & Request Code -->
        <div id="delete-step-1">
            <i class="fas fa-exclamation-triangle" style="font-size: 3.5em; color: var(--red); margin-bottom: 20px;"></i>
            <h2 style="color: var(--red); margin-bottom: 15px; font-size: 1.8em;">Delete Account?</h2>
            <p style="margin-bottom: 25px; color: var(--silver); line-height: 1.6;">
                Are you absolutely sure? Account deletion is permanent. We will send a 6-digit confirmation code to your email (<strong style="color: var(--white);"><%= user.email %></strong>).
            </p>
            
            <div style="background: rgba(0,0,0,0.3); padding: 15px; border-radius: 8px; border: 1px solid #333; margin-bottom: 25px;">
                <label style="display: flex; align-items: center; justify-content: center; gap: 12px; cursor: pointer; margin: 0;">
                    <input type="checkbox" id="preserve-mods" name="preserveMods" value="true" style="width: 20px; height: 20px; accent-color: var(--gold);">
                    <span style="color: var(--white); font-weight: 500;">Preserve my uploaded mods.</span>
                </label>
            </div>
            
            <div style="display: flex; gap: 15px; justify-content: center;">
                <button type="button" id="send-code-btn" class="btn btn-primary" style="background-color: var(--red); color: var(--white); border: none; box-shadow: 0 0 15px var(--glow-red); padding: 12px 25px; font-weight: bold; cursor: pointer;">
                    <i class="fas fa-paper-plane" style="margin-right: 8px;"></i> Send Code
                </button>
                <button type="button" class="btn btn-secondary cancelDeleteBtn" style="padding: 12px 25px;">Cancel</button>
            </div>
        </div>

        <!-- STEP 2: Enter OTP Code & Confirm -->
        <div id="delete-step-2" style="display: none;">
            <i class="fas fa-envelope-open-text" style="font-size: 3.5em; color: var(--gold); margin-bottom: 20px;"></i>
            <h2 style="color: var(--white); margin-bottom: 15px; font-size: 1.8em;">Enter Confirmation Code</h2>
            <p style="margin-bottom: 25px; color: var(--silver); line-height: 1.6;">
                We sent a 6-digit security code to <strong style="color: var(--white);"><%= user.email %></strong>. Enter the code below to confirm account deletion.
            </p>

            <div style="margin-bottom: 25px;">
                <input type="text" id="delete-otp-input" maxlength="6" placeholder="123456" autocomplete="off" style="width: 100%; max-width: 260px; padding: 14px 20px; font-size: 1.8em; letter-spacing: 10px; text-align: center; font-weight: bold; border-radius: 10px; border: 2px solid var(--gold); background: #0a0a0a; color: var(--gold); outline: none; box-shadow: 0 0 15px rgba(255, 215, 0, 0.15);">
            </div>

            <div style="display: flex; flex-direction: column; gap: 15px; align-items: center;">
                <div style="display: flex; gap: 15px; justify-content: center; width: 100%;">
                    <button type="button" id="confirm-delete-btn" class="btn btn-primary" style="background-color: var(--red); color: var(--white); border: none; box-shadow: 0 0 15px var(--glow-red); padding: 12px 25px; font-weight: bold; cursor: pointer;">
                        <i class="fas fa-trash-alt" style="margin-right: 8px;"></i> Confirm & Delete
                    </button>
                    <button type="button" class="btn btn-secondary cancelDeleteBtn" style="padding: 12px 25px;">Cancel</button>
                </div>

                <button type="button" id="resend-code-btn" style="background: transparent; border: none; color: var(--silver); text-decoration: underline; font-size: 0.9em; cursor: pointer; transition: color 0.3s ease; margin-top: 5px;" onmouseover="this.style.color='var(--gold)';" onmouseout="this.style.color='var(--silver)';">
                    <i class="fas fa-redo-alt" style="margin-right: 5px;"></i> Didn't receive code? Resend Code
                </button>
            </div>
        </div>

    </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', () => {
    // Helper to animate modals
    function openModal(modal) {
        if (!modal) return;
        modal.style.display = 'flex';
        setTimeout(() => modal.classList.add('show'), 10);
    }
    function closeModal(modal) {
        if (!modal) return;
        modal.classList.remove('show');
        setTimeout(() => modal.style.display = 'none', 200);
    }

    // Newsletter Modal
    const newsModal = document.getElementById('newsletter-modal');
    const openNewsBtn = document.getElementById('open-newsletter-modal');
    if (openNewsBtn) openNewsBtn.addEventListener('click', () => openModal(newsModal));
    document.getElementById('cancel-newsletter-btn')?.addEventListener('click', () => closeModal(newsModal));

    // Logout Modal
    const logoutModal = document.getElementById('logout-modal-container');
    const logoutBtn = document.getElementById('logout-btn');
    if (logoutBtn) logoutBtn.addEventListener('click', () => openModal(logoutModal));
    document.getElementById('cancelLogoutBtn')?.addEventListener('click', () => closeModal(logoutModal));

    // Leave Partnership Modal
    const leavePartnershipModal = document.getElementById('leave-partnership-modal-container');
    const openLeaveBtn = document.getElementById('open-leave-partnership-modal-btn');
    if (openLeaveBtn) openLeaveBtn.addEventListener('click', () => openModal(leavePartnershipModal));
    document.getElementById('cancel-leave-partnership-btn')?.addEventListener('click', () => closeModal(leavePartnershipModal));
    document.getElementById('confirm-leave-partnership-btn')?.addEventListener('click', async () => {
        const btn = document.getElementById('confirm-leave-partnership-btn');
        btn.disabled = true;
        btn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Leaving...';
        try {
            const response = await fetch('/partnership/leave', { method: 'POST', headers: { 'Content-Type': 'application/json' } });
            const data = await response.json();
            if (data.success) {
                window.location.reload();
            } else {
                alert(data.message || 'Failed to leave partnership.');
                btn.disabled = false;
                btn.innerHTML = 'Yes, I want to leave';
            }
        } catch (err) {
            alert('Network error occurred.');
            btn.disabled = false;
            btn.innerHTML = 'Yes, I want to leave';
        }
    });

    // Delete Modal Controls

    const deleteModal = document.getElementById('delete-modal-container');
    const openDeleteBtn = document.getElementById('open-delete-modal-btn');
    const sendCodeBtn = document.getElementById('send-code-btn');
    const confirmDeleteBtn = document.getElementById('confirm-delete-btn');
    const resendCodeBtn = document.getElementById('resend-code-btn');
    const deleteStep1 = document.getElementById('delete-step-1');
    const deleteStep2 = document.getElementById('delete-step-2');
    const deleteAlert = document.getElementById('delete-alert');
    const deleteOtpInput = document.getElementById('delete-otp-input');
    const preserveModsCheckbox = document.getElementById('preserve-mods');

    function resetDeleteModal() {
        if (deleteStep1) deleteStep1.style.display = 'block';
        if (deleteStep2) deleteStep2.style.display = 'none';
        if (deleteAlert) {
            deleteAlert.style.display = 'none';
            deleteAlert.className = '';
            deleteAlert.innerText = '';
        }
        if (deleteOtpInput) deleteOtpInput.value = '';
        if (sendCodeBtn) {
            sendCodeBtn.disabled = false;
            sendCodeBtn.innerHTML = '<i class="fas fa-paper-plane" style="margin-right: 8px;"></i> Send Code';
        }
        if (confirmDeleteBtn) {
            confirmDeleteBtn.disabled = false;
            confirmDeleteBtn.innerHTML = '<i class="fas fa-trash-alt" style="margin-right: 8px;"></i> Confirm & Delete';
        }
    }

    function showDeleteAlert(msg, type = 'error') {
        if (!deleteAlert) return;
        deleteAlert.innerText = msg;
        deleteAlert.style.display = 'block';
        if (type === 'success') {
            deleteAlert.style.background = 'rgba(76, 175, 80, 0.2)';
            deleteAlert.style.border = '1px solid #4CAF50';
            deleteAlert.style.color = '#81c784';
        } else {
            deleteAlert.style.background = 'rgba(229, 57, 53, 0.2)';
            deleteAlert.style.border = '1px solid #e53935';
            deleteAlert.style.color = '#ef5350';
        }
    }

    if (openDeleteBtn) {
        openDeleteBtn.addEventListener('click', () => {
            resetDeleteModal();
            openModal(deleteModal);
        });
    }

    document.querySelectorAll('.cancelDeleteBtn').forEach(btn => {
        btn.addEventListener('click', () => {
            closeModal(deleteModal);
            setTimeout(resetDeleteModal, 250);
        });
    });

    // Request Code (Step 1 -> Step 2) or Resend Code
    async function requestDeletionCode(isResend = false) {
        const targetBtn = isResend ? resendCodeBtn : sendCodeBtn;
        if (targetBtn) {
            targetBtn.disabled = true;
            targetBtn.innerHTML = isResend ? '<i class="fas fa-spinner fa-spin"></i> Resending...' : '<i class="fas fa-spinner fa-spin"></i> Sending Code...';
        }

        try {
            const response = await fetch('/account/delete-request', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' }
            });
            const data = await response.json();

            if (data.success) {
                if (deleteStep1) deleteStep1.style.display = 'none';
                if (deleteStep2) deleteStep2.style.display = 'block';
                showDeleteAlert(isResend ? 'A new confirmation code has been sent to your email.' : 'Confirmation code sent to your email. Please check your inbox.', 'success');
                if (deleteOtpInput) deleteOtpInput.focus();
            } else {
                showDeleteAlert(data.message || 'Failed to send confirmation code. Please try again.');
            }
        } catch (err) {
            console.error("Delete request error:", err);
            showDeleteAlert('Network or server error. Please try again.');
        } finally {
            if (sendCodeBtn) {
                sendCodeBtn.disabled = false;
                sendCodeBtn.innerHTML = '<i class="fas fa-paper-plane" style="margin-right: 8px;"></i> Send Code';
            }
            if (resendCodeBtn) {
                resendCodeBtn.disabled = false;
                resendCodeBtn.innerHTML = '<i class="fas fa-redo-alt" style="margin-right: 5px;"></i> Didn\'t receive code? Resend Code';
            }
        }
    }

    if (sendCodeBtn) sendCodeBtn.addEventListener('click', () => requestDeletionCode(false));
    if (resendCodeBtn) resendCodeBtn.addEventListener('click', () => requestDeletionCode(true));

    // Confirm Code & Delete Account
    if (confirmDeleteBtn) {
        confirmDeleteBtn.addEventListener('click', async () => {
            const otp = deleteOtpInput ? deleteOtpInput.value.trim() : '';
            if (!otp || otp.length !== 6) {
                return showDeleteAlert('Please enter a valid 6-digit confirmation code.');
            }

            confirmDeleteBtn.disabled = true;
            confirmDeleteBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Deleting Account...';
            deleteAlert.style.display = 'none';

            try {
                const preserveMods = preserveModsCheckbox ? preserveModsCheckbox.checked : false;
                const response = await fetch('/account/delete-confirm', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ otp, preserveMods })
                });
                const data = await response.json();

                if (data.success && data.redirect) {
                    window.location.href = data.redirect;
                } else {
                    showDeleteAlert(data.message || 'Verification failed. Please check your code.');
                    confirmDeleteBtn.disabled = false;
                    confirmDeleteBtn.innerHTML = '<i class="fas fa-trash-alt" style="margin-right: 8px;"></i> Confirm & Delete';
                }
            } catch (err) {
                console.error("Delete confirm error:", err);
                showDeleteAlert('Network error. Account deletion failed.');
                confirmDeleteBtn.disabled = false;
                confirmDeleteBtn.innerHTML = '<i class="fas fa-trash-alt" style="margin-right: 8px;"></i> Confirm & Delete';
            }
        });
    }

    // Close Modals on outside click
    window.addEventListener('click', (e) => {
        if (e.target === newsModal) closeModal(newsModal);
        if (e.target === logoutModal) closeModal(logoutModal);
        if (e.target === deleteModal) {
            closeModal(deleteModal);
            setTimeout(resetDeleteModal, 250);
        }
    });

    // --- Music Settings AJAX ---
    const allowThemeCheckbox = document.getElementById('setting-allow-theme');
    const autoPlayCheckbox = document.getElementById('setting-autoplay-theme');

    async function updateMusicSettings() {
        try {
            const response = await fetch('/api/user/music-settings', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({
                    allowThemeMusic: allowThemeCheckbox.checked,
                    autoPlayTheme: autoPlayCheckbox.checked
                })
            });
            const data = await response.json();
            if (!data.success) {
                alert('Failed to update music settings.');
            }
        } catch (err) {
            console.error('Music settings error:', err);
            alert('Network error occurred.');
        }
    }

    if (allowThemeCheckbox) allowThemeCheckbox.addEventListener('change', updateMusicSettings);
    if (autoPlayCheckbox) autoPlayCheckbox.addEventListener('change', updateMusicSettings);

    // --- Web Cache Cleaner Script ---
    const purgeCacheBtn = document.getElementById('btn-purge-cache');
    const cacheStatus = document.getElementById('cache-clean-status');

    if (purgeCacheBtn) {
        purgeCacheBtn.addEventListener('click', async () => {
            if (!confirm('Clear all local web cache, service workers, and temporary offline storage? This will perform a fresh page reload.')) return;
            
            purgeCacheBtn.disabled = true;
            purgeCacheBtn.innerHTML = '<i class="fas fa-spinner fa-spin" style="margin-right: 6px;"></i> Cleaning Cache...';
            if (cacheStatus) {
                cacheStatus.style.display = 'block';
                cacheStatus.style.color = 'var(--gold)';
                cacheStatus.textContent = 'Purging Service Workers & CacheStorage...';
            }

            try {
                // 1. Service Workers
                if ('serviceWorker' in navigator) {
                    const registrations = await navigator.serviceWorker.getRegistrations();
                    for (const reg of registrations) {
                        await reg.unregister();
                    }
                }

                // 2. CacheStorage
                if ('caches' in window) {
                    const keys = await caches.keys();
                    await Promise.all(keys.map(k => caches.delete(k)));
                }

                // 3. Clear Storage
                try { localStorage.clear(); } catch(e){}
                try { sessionStorage.clear(); } catch(e){}

                if (cacheStatus) {
                    cacheStatus.style.color = 'var(--green)';
                    cacheStatus.textContent = '✔ All caches cleared! Reloading fresh...';
                }

                setTimeout(() => {
                    window.location.replace(window.location.pathname + '?_cc=' + Date.now() + '&cache_cleared=1');
                }, 500);
            } catch (err) {
                console.error("Cache purge failed:", err);
                if (cacheStatus) {
                    cacheStatus.style.color = 'var(--red)';
                    cacheStatus.textContent = '✖ Error clearing cache: ' + err.message;
                }
                purgeCacheBtn.disabled = false;
                purgeCacheBtn.innerHTML = '<i class="fas fa-trash-alt" style="margin-right: 6px;"></i> Clear Web Cache Now';
            }
        });
    }
});
</script>

<%- include('../partials/footer') %>
