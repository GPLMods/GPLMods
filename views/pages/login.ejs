<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - GPL Mods</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <!-- Add reCAPTCHA script -->
    <script src="https://www.google.com/recaptcha/api.js" async defer></script>
    
    <style>
        :root {
            --black: #0a0a0a;
            --dark-gray: #1a1a1a;
            --white: #ffffff;
            --gold: #FFD700;
            --silver: #c0c0c0;
            --glow-gold: rgba(255, 215, 0, 0.5);
            --error: #ff4d4d;
            --success: #2ecc71;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body { 
            font-family: 'Poppins', sans-serif; 
            background-color: var(--black); 
            color: var(--white); 
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        /* Form Container - Matches Sign Up */
        .auth-container { 
            width: 100%;
            max-width: 450px; 
            background-color: var(--dark-gray); 
            padding: 40px; 
            border-radius: 15px; 
            border: 1px solid #2a2a2a;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
        }

        .logo-area {
            text-align: center;
            margin-bottom: 20px;
            font-size: 1.8em;
            font-weight: 700;
            color: var(--gold);
            text-decoration: none;
            display: block;
        }
        .logo-area span { color: var(--silver); }

        .form-container h2 { text-align: center; font-size: 1.8em; margin-bottom: 25px; }
        
        .form-group { margin-bottom: 20px; position: relative; }
        .form-group label { display: block; margin-bottom: 8px; font-weight: 600; color: var(--silver); font-size: 0.9em; }
        .form-group input { width: 100%; padding: 12px 15px; border-radius: 8px; border: 1px solid #333; background-color: var(--black); color: var(--white); font-size: 1em; }
        .form-group input:focus { outline: none; border-color: var(--gold); box-shadow: 0 0 10px var(--glow-gold); }

        .submit-button { 
            display: block; 
            width: 100%; 
            background: var(--gold); 
            color: var(--black); 
            padding: 14px 20px; 
            border-radius: 8px; 
            font-size: 1.1em; 
            font-weight: 700; 
            border: none; 
            cursor: pointer; 
            transition: all 0.3s ease; 
            margin-top: 10px;
        }
        .submit-button:hover { transform: translateY(-2px); box-shadow: 0 0 20px var(--glow-gold); }

        /* Divider - Matches Sign Up */
        .divider {
            text-align: center;
            margin: 20px 0;
            position: relative;
            color: #666;
            font-size: 0.9em;
        }
        .divider::before, .divider::after {
            content: "";
            position: absolute;
            top: 50%;
            width: 40%;
            height: 1px;
            background: #333;
        }
        .divider::before { left: 0; }
        .divider::after { right: 0; }

        /* Google Button - Matches Sign Up */
        .google-btn {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 100%;
            background-color: var(--white);
            color: #000;
            padding: 12px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1em;
            transition: background 0.3s;
            border: none;
            cursor: pointer;
        }
        .google-btn img {
            width: 20px;
            margin-right: 10px;
        }
        .google-btn:hover {
            background-color: #f1f1f1;
        }

        .form-switch { text-align: center; margin-top: 25px; color: var(--silver); font-size: 0.9em; }
        .form-switch a { color: var(--gold); text-decoration: none; font-weight: 600; }
        .form-switch a:hover { text-decoration: underline; }

        /* Adjust reCAPTCHA alignment if needed */
        .g-recaptcha {
            display: flex;
            justify-content: center;
            margin-bottom: 15px;
        }

        @media (max-width: 480px) {
            .auth-container { padding: 25px; border-radius: 0; height: 100vh; border: none; }
        }
    </style>
</head>
<body>

    <div class="auth-container">
        <!-- Changed index.html to / -->
        <a href="/" class="logo-area">GPL <span>Mods</span></a>
        
        <section class="form-container">
            <h2>Welcome Back</h2>

            <!-- EJS Dynamic Message Block -->
            <% if (message) { %>
                <p style="padding: 10px; background-color: #2b1a1a; border: 1px solid var(--gold); text-align: center; margin-bottom: 1rem; border-radius: 8px; font-size: 0.9em;">
                    <%= message %>
                </p>
            <% } %>

            <!-- Updated Form Attributes -->
            <form id="login-form" action="/login" method="POST">
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="email" id="email" name="email" placeholder="e.g. modmaster@example.com" required>
                </div>

                <div class="form-group">
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" placeholder="••••••••" required>
                </div>

                <!-- reCAPTCHA integration -->
                <div class="form-group">
                    <div class="g-recaptcha" data-sitekey="<%= recaptchaSiteKey %>"></div>
                </div>

                <button type="submit" class="submit-button">Login</button>
            </form>

            <div class="divider">or</div>

            <!-- Google Login Link -->
            <a href="/auth/google" class="google-signin-btn">
    <svg class="google-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 48">
        <path fill="#FFC107" d="M43.611,20.083H42V20H24v8h11.303c-1.649,4.657-6.08,8-11.303,8c-6.627,0-12-5.373-12-12s5.373-12,12-12c3.059,0,5.842,1.154,7.961,3.039l5.657-5.657C34.046,6.053,29.268,4,24,4C12.955,4,4,12.955,4,24s8.955,20,20,20s20-8.955,20-20C44,22.659,43.862,21.35,43.611,20.083z"/>
        <path fill="#FF3D00" d="M6.306,14.691l6.571,4.819C14.655,15.108,18.961,12,24,12c3.059,0,5.842,1.154,7.961,3.039l5.657-5.657C34.046,6.053,29.268,4,24,4C16.318,4,9.656,8.337,6.306,14.691z"/>
        <path fill="#4CAF50" d="M24,44c5.166,0,9.86-1.977,13.409-5.192l-6.19-5.238C29.211,35.091,26.715,36,24,36c-5.202,0-9.619-3.317-11.283-7.946l-6.522,5.025C9.505,39.556,16.227,44,24,44z"/>
        <path fill="#1976D2" d="M43.611,20.083L43.595,20L42,20H24v8h11.303c-0.792,2.237-2.231,4.166-4.087,5.574l6.19,5.238C39.978,36.316,44,30.659,44,24C44,22.659,43.862,21.35,43.611,20.083z"/>
    </svg>
    <span>Continue with Google</span>
</a>

            <!-- Changed signup.html to /register -->
            <p class="form-switch">Need an account? <a href="/register">Sign Up</a></p>
        </section>
    </div>

</body>
</html>